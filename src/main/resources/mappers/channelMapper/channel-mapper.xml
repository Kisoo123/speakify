<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Channel">
    <insert id="inviteToChannel" parameterType="map">
        insert into channelInvite(status, channelid, alarmid, touser, fromuser)
        values ('PENDING',${roomNo},)
    </insert>
    <select id="selectInviteRequest" parameterType="map" resultType="com.project.channel.model.dto.ChannelInvite">
        select * from channelInvite
        where fromUser=${userId} and toUser = ${friendId} and channelId=${roomNo}
    </select>
</mapper>